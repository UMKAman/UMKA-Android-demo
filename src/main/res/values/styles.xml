<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.Translucent" />

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ratingBarBig" parent="@android:style/Widget.RatingBar">

        <item name="android:indeterminateDrawable">@drawable/star_big</item>
        <item name="android:progressDrawable">@drawable/star_big</item>
    </style>

    <style name="ratingBarSmall" parent="@android:style/Widget.Holo.Light.RatingBar.Small">
        <item name="android:indeterminateDrawable">@drawable/star_smal</item>
        <item name="android:progressDrawable">@drawable/star_smal</item>
    </style>
</resources>
